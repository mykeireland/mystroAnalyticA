name: Deploy static site to $web

on:
  push:
    branches: [ main ]
    paths:
      - 'index/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Upload to $web
        uses: azure/cli@v2
        with:
          inlineScript: |
            az storage blob upload-batch \
              --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
              --account-key  ${{ secrets.AZURE_STORAGE_KEY }} \
              --destination '$web' \
              --source index \
              --no-progress

            # set no-cache for html/js so users see updates immediately
            az storage blob update \
              --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
              --account-key  ${{ secrets.AZURE_STORAGE_KEY }} \
              --container-name '$web' --name index.html \
              --content-cache-control "no-store, no-cache, must-revalidate" || true

            az storage blob update \
              --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
              --account-key  ${{ secrets.AZURE_STORAGE_KEY }} \
              --container-name '$web' --name script.js \
              --content-cache-control "no-store, no-cache, must-revalidate" || true
